{
    "title": "Person",
    "type": "object",
    "description": "",
    "properties": {
        "age": {
            "maximum": 200,
            "minimum": 0,
            "type": "integer"
        },
        "aliases": {
            "description": "other names for the person",
            "items": {
                "type": "string"
            },
            "type": "array"
        },
        "full_name": {
            "description": "name of the person",
            "type": "string"
        },
        "phone": {
            "pattern": "^[\\d\\(\\)\\-]+$",
            "type": "string"
        }
    },
    "required": [
        "full_name"
    ]
}

